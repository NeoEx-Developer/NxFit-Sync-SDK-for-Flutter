name: nxfit_sync_sdk
version: 0.0.11
description: >
  Seamlessly sync health data from iOS HealthKit and Android Health Connect to the NxFit platform.
homepage: https://www.neoex.io/
repository: https://github.com/NeoEx-Developer/NxFit-Sync-SDK-for-Flutter

environment:
  sdk: ^3.8.1
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.9.0
  logging: ^1.3.0
  nxfit_sdk: ^0.4.0
  plugin_platform_interface: ^2.0.2
  rxdart: ^0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: '>=2.3.0 <4.0.0'
  json_serializable: ^6.8.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.neoex.nxfit.sync_sdk
        pluginClass: NxfitSyncSdkPlugin
      ios:
        pluginClass: NxfitSyncSdkPlugin
